# Port für den HTTP-API-Server
PORT=8080

# Optionaler erlaubter CORS-Origin (z. B. https://example.github.io)
# Wenn nicht gesetzt, wird CORS permissiv konfiguriert.
ALLOWED_ORIGIN=

# Logging-Konfiguration für tracing-subscriber
RUST_LOG=info
